<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.kryex.phonerepair.mapper.PartsMapper">
    <select id="list" resultType="cn.kryex.phonerepair.entity.po.Parts">
        SELECT parts.* FROM yjx_parts parts
        <where>
            <if test="userId != null">
                AND ( #{userId} IN (1,5,8) )
            </if>
            <if test="searchKeyword != null and searchKeyword != ''">
                AND (
                    parts.part_name LIKE CONCAT('%', #{searchKeyword}, '%') OR
                    parts.part_description LIKE CONCAT('%', #{searchKeyword}, '%')
                )
            </if>
        </where>
        ORDER BY ${orderBy}
    </select>

    <insert id="insert" parameterType="cn.kryex.phonerepair.entity.po.Parts">
        INSERT INTO yjx_parts (part_name, part_description, part_price, stock_quantity, supplier_id)
        VALUES (#{partName}, #{partDescription}, #{partPrice}, #{stockQuantity}, #{supplierId})
    </insert>

    <update id="updateById" parameterType="cn.kryex.phonerepair.entity.po.Parts">
        UPDATE yjx_parts
        SET part_name = #{partName},
            part_description = #{partDescription},
            part_price = #{partPrice},
            stock_quantity = #{stockQuantity},
            supplier_id = #{supplierId}
        WHERE part_id = #{partId}
    </update>
    
    <delete id="deleteById" parameterType="int">
        DELETE FROM yjx_parts WHERE part_id = #{partId}
    </delete>
</mapper>